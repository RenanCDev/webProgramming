export default function Rota1Layout({ children }) {
    return (
      <div>
        <header>
          <h1>Bem-vindo à Rota 3</h1>
        </header>
        <main>
          {children}
        </main>
      </div>
    );
  }
  